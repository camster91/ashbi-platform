# tests/e2e/docker-compose.test.yml
#
# Hub stack for the magic-login end-to-end smoke test.
#
# Services:
#   postgres  — fresh DB per test run (no volume)
#   migrate   — one-shot `npx prisma db push` against the test DB.
#               Prisma 7 reads the URL from prisma.config.ts datasource
#               block, but `dotenv/config` only sets vars present in .env
#               — it doesn't synthesize them. Compose sets the env var
#               but doesn't write the .env file. So we materialize the
#               URL into /app/.env via the entrypoint before invoking
#               db push. --url flag is also passed as a belt-and-suspenders
#               override.
#   seed      — one-shot `node prisma/seed.js` after migrate completes.
#               Creates cameron@ashbi.ca with the role ADMIN so the test
#               can log in via /api/auth/login.
#   hub       — ashbi-platform image running Fastify. depends_on
#               postgres (healthy) + migrate (succeeded) + seed (succeeded).
#
# WP_BRIDGE_SECRET matches ashbi_secret_key in tests/e2e/wp-env/setup-plugin.sh.
# ADMIN_SEED_PASSWORD matches ADMIN_PASSWORD the test logs in with.
#
# Usage:
#   docker compose -f tests/e2e/docker-compose.test.yml up -d
#   docker compose -f tests/e2e/docker-compose.test.yml down -v

services:
  postgres:
    # Prisma declares the vector extension, so the disposable test database
    # must provide pgvector rather than plain PostgreSQL.
    image: pgvector/pgvector:0.8.2-pg16-bookworm
    restart: unless-stopped
    environment:
      POSTGRES_USER: ashbi_test
      POSTGRES_PASSWORD: ashbi_test
      POSTGRES_DB: ashbi_test
    # No volumes — fresh DB per run.
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ashbi_test -d ashbi_test"]
      interval: 2s
      timeout: 3s
      retries: 30
    ports:
      - "54329:5432"

  migrate:
    build:
      context: ../..
      dockerfile: Dockerfile
    restart: "no"
    # Test-only setup needs devDependencies (Prisma CLI) and writes a
    # disposable .env. The long-running hub remains the non-root image user.
    user: root
    entrypoint: ["/bin/sh", "-c"]
    command:
      - |
        set -e
        npm ci --no-audit --no-fund
        printf 'DATABASE_URL="%s"\n' "$$DATABASE_URL" > /app/.env
        export DATABASE_URL="$$DATABASE_URL"
        exec npx prisma db push --url "$$DATABASE_URL" --accept-data-loss
    depends_on:
      postgres:
        condition: service_healthy
    environment:
      DATABASE_URL: postgresql://ashbi_test:ashbi_test@postgres:5432/ashbi_test

  seed:
    build:
      context: ../..
      dockerfile: Dockerfile
    restart: "no"
    # Test-only setup installs the locked dev tree for the seed helper.
    user: root
    command: ["sh", "-c", "npm ci --no-audit --no-fund && npx prisma generate && node prisma/seed.js"]
    depends_on:
      migrate:
        condition: service_completed_successfully
      postgres:
        condition: service_healthy
    environment:
      DATABASE_URL: postgresql://ashbi_test:ashbi_test@postgres:5432/ashbi_test
      ADMIN_SEED_PASSWORD: TestPass123!

  hub:
    build:
      context: ../..
      dockerfile: Dockerfile
    restart: unless-stopped
    extra_hosts:
      - "host.docker.internal:host-gateway"
    depends_on:
      postgres:
        condition: service_healthy
      migrate:
        condition: service_completed_successfully
      seed:
        condition: service_completed_successfully
    environment:
      NODE_ENV: test
      PORT: 3000
      DATABASE_URL: postgresql://ashbi_test:ashbi_test@postgres:5432/ashbi_test
      JWT_SECRET: test-jwt-secret-do-not-use-in-prod
      ADMIN_EMAIL: cameron@ashbi.ca
      ADMIN_PASSWORD: TestPass123!
      WP_BRIDGE_SECRET: test-e2e-secret-12345
      MAGIC_LOGIN_AUDIT_MAX: 500
      MAGIC_LOGIN_RATE_LIMIT_PER_HOUR: 5
      FLEET_DIGEST_ENABLED: "false"
    ports:
      - "${HOST_PORT:-3001}:3000"
    healthcheck:
      test: ["CMD", "wget", "-qO-", "http://localhost:3000/api/health"]
      interval: 3s
      timeout: 5s
      retries: 20
      start_period: 10s
