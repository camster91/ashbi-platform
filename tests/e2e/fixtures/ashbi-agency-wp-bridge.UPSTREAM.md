# ashbi-agency-wp-bridge E2E fixture

This directory is a test-only snapshot of the private
`camster91/ashbi-agency-wp-bridge` repository at commit
`3289bbc798cad233f00e7c006c7c2645ed86aeef`.

It is committed here so the E2E suite is deterministic and does not require a
GitHub credential or network clone at runtime. Update the fixture and this
commit reference together after validating the magic-login contract.
