# Archived pre-baseline migrations

These migrations are retained only as historical evidence. They are not a
valid executable chain: the first retained migration alters tables that it
never creates.

The active migration history starts at
`prisma/migrations/20260808040000_baseline`. Never copy these archived files
back into the active migration directory or apply them to a database.
